import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 24,
    justifyContent: 'center',
  },
  heading: {
    fontSize: 22,
    marginBottom: 12,
    fontWeight: '600',
  },
  hint: {
    marginBottom: 24,
    opacity: 0.75,
  },
  gap: {
    height: 12,
  },
});
